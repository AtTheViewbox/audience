name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - staging
      - medgemma
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: audience/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: audience

      - name: Build
        run: |
          if [ "${{ github.ref }}" == "refs/heads/staging" ]; then
            export BUILD_ENV=staging
          elif [ "${{ github.ref }}" == "refs/heads/medgemma" ]; then
            export BUILD_ENV=medgemma
          fi
          npm run build
        working-directory: audience

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: audience/dist
          target-folder: ${{ github.ref == 'refs/heads/staging' && 'staging' || (github.ref == 'refs/heads/medgemma' && 'medgemma' || '.') }}
          clean: true
          clean-exclude: ${{ github.ref == 'refs/heads/main' && 'staging medgemma' || '' }}
