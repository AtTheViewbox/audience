name: Deploy medgemma to GitHub Pages

on:
  push:
    branches:
      - medgemma

permissions:
  contents: write

jobs:
  build-and-deploy:
    concurrency: ci-medgemma
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build:medgemma

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          target-folder: medgemma
          clean: true
          clean-exclude: |
            .nojekyll
